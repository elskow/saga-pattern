---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: saga-test
  labels:
    app: order-service
    pattern: orchestration
spec:
  type: NodePort
  ports:
    - port: 8085
      targetPort: 8085
      nodePort: 30085
      name: http
  selector:
    app: order-service
    pattern: orchestration
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: saga-test
  labels:
    app: order-service
    pattern: orchestration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
      pattern: orchestration
  template:
    metadata:
      labels:
        app: order-service
        pattern: orchestration
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: order-service
          image: ghcr.io/elskow/saga-pattern/orchestration-order-service:latest
          ports:
            - containerPort: 8085
              name: http
          env:
            - name: SERVER_PORT
              value: "8085"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://order-db.saga-infra.svc.cluster.local:5432/order_db"
            - name: SPRING_DATASOURCE_USERNAME
              value: "postgres"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: password
            - name: EVENTUATELOCAL_KAFKA_BOOTSTRAP_SERVERS
              value: "kafka.saga-infra.svc.cluster.local:9092"
            - name: EVENTUATELOCAL_ZOOKEEPER_CONNECTION_STRING
              value: "zookeeper.saga-infra.svc.cluster.local:2181"
            - name: ZIPKIN_ENDPOINT
              value: "http://zipkin.saga-infra.svc.cluster.local:9411/api/v2/spans"
            - name: LOG_LEVEL
              value: "INFO"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 90
            periodSeconds: 10
