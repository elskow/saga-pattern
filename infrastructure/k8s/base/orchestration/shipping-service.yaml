---
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: saga-test
  labels:
    app: shipping-service
    pattern: orchestration
spec:
  ports:
    - port: 8088
      targetPort: 8088
      name: http
  selector:
    app: shipping-service
    pattern: orchestration
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: saga-test
  labels:
    app: shipping-service
    pattern: orchestration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shipping-service
      pattern: orchestration
  template:
    metadata:
      labels:
        app: shipping-service
        pattern: orchestration
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: shipping-service
          image: ghcr.io/elskow/saga-pattern/orchestration-shipping-service:latest
          ports:
            - containerPort: 8088
              name: http
          env:
            - name: SERVER_PORT
              value: "8088"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://shipping-db.saga-infra.svc.cluster.local:5432/shipping_db"
            - name: SPRING_DATASOURCE_USERNAME
              value: "postgres"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: password
            - name: EVENTUATELOCAL_KAFKA_BOOTSTRAP_SERVERS
              value: "kafka.saga-infra.svc.cluster.local:9092"
            - name: EVENTUATELOCAL_ZOOKEEPER_CONNECTION_STRING
              value: "zookeeper.saga-infra.svc.cluster.local:2181"
            - name: ZIPKIN_ENDPOINT
              value: "http://zipkin.saga-infra.svc.cluster.local:9411/api/v2/spans"
            - name: LOG_LEVEL
              value: "INFO"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8088
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8088
            initialDelaySeconds: 90
            periodSeconds: 10
