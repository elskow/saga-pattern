ALTER TABLE saga_instances
    ADD COLUMN IF NOT EXISTS benchmark_run VARCHAR(255) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS benchmark_scene VARCHAR(255) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS benchmark_phase VARCHAR(255) NOT NULL DEFAULT '';

ALTER TABLE outbox_messages
    ADD COLUMN IF NOT EXISTS benchmark_run VARCHAR(255) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS benchmark_scene VARCHAR(255) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS benchmark_phase VARCHAR(255) NOT NULL DEFAULT '';
